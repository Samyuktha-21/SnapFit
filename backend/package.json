{
  "name": "snapfit-backend",
  "version": "1.0.0",
  "description": "SnapFit size-calculation API. One endpoint that turns MediaPipe pose landmarks into a clothing size.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
